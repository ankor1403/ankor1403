# 🚀 ПРОСТАЯ ИНСТРУКЦИЯ ДЛЯ НАЧИНАЮЩИХ

## ✨ Что это?

Это набор инструментов для исправления проблем с вашим Telegram сервисом на сервере **83.222.25.94**.

**Проблема:** Не работает авторизация по QR коду и отправка кода подтверждения Telegram.

**Решение:** Один скрипт, который всё сделает автоматически!

---

## 📋 ВАМ ПОНАДОБИТСЯ

- ✅ Компьютер с Windows или Linux
- ✅ Доступ в Интернет
- ✅ SSH доступ к серверу (у вас уже есть)

---

## 🎯 ВАРИАНТ 1: АВТОМАТИЧЕСКАЯ УСТАНОВКА (САМЫЙ ПРОСТОЙ)

### Для Windows:

#### Шаг 1: Откройте PowerShell
Нажмите `Win + R`, введите `powershell` и нажмите Enter

#### Шаг 2: Скопируйте и вставьте эту ОДНУ команду:

```powershell
iwr -useb "https://raw.githubusercontent.com/ankor1403/ankor1403/claude/audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm/install_and_run.bat" -OutFile "$env:TEMP\install_and_run.bat"; & "$env:TEMP\install_and_run.bat"
```

#### Шаг 3: Нажмите Enter и ждите
Скрипт автоматически:
- ✅ Скачает все файлы
- ✅ Запустит диагностику
- ✅ Предложит исправить проблемы

---

### Для Linux/Mac:

#### Шаг 1: Откройте Терминал

#### Шаг 2: Скопируйте и вставьте эту ОДНУ команду:

```bash
curl -sSL "https://raw.githubusercontent.com/ankor1403/ankor1403/claude/audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm/install_and_run.sh" | bash
```

#### Шаг 3: Нажмите Enter и ждите
Всё произойдёт автоматически!

---

## 🎯 ВАРИАНТ 2: РУЧНАЯ УСТАНОВКА

Если автоматическая установка не работает:

### Для Windows:

1. **Скачайте файл вручную:**
   - Откройте: https://github.com/ankor1403/ankor1403/archive/refs/heads/claude/audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm.zip
   - Нажмите "Скачать" (Download)

2. **Разархивируйте:**
   - Щёлкните правой кнопкой по скачанному файлу
   - Выберите "Извлечь всё..." (Extract All)
   - Запомните, куда извлекли файлы

3. **Откройте командную строку:**
   - Нажмите `Win + R`
   - Введите `cmd`
   - Нажмите Enter

4. **Перейдите в папку с файлами:**
   ```cmd
   cd C:\Users\ВашеИмя\Downloads\ankor1403-claude-audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm
   ```
   (Замените путь на тот, куда вы извлекли файлы)

5. **Запустите диагностику:**
   ```cmd
   bash telegram_diagnostic.sh
   ```

6. **Если нужно исправить проблемы:**
   ```cmd
   bash fix_telegram_service.sh
   ```

---

### Для Linux:

1. **Скачайте репозиторий:**
   ```bash
   cd ~
   git clone https://github.com/ankor1403/ankor1403.git
   cd ankor1403
   git checkout claude/audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm
   ```

2. **Сделайте скрипты исполняемыми:**
   ```bash
   chmod +x *.sh
   ```

3. **Запустите диагностику:**
   ```bash
   ./telegram_diagnostic.sh
   ```

4. **Если нужно исправить проблемы:**
   ```bash
   ./fix_telegram_service.sh
   ```

---

## 📁 ЧТО БУДЕТ СКАЧАНО

После запуска у вас появятся эти файлы:

- 📄 **telegram_diagnostic.sh** - Проверяет, что не работает
- 📄 **fix_telegram_service.sh** - Исправляет проблемы автоматически
- 📄 **test_telegram_auth.py** - Тестирует авторизацию Telegram
- 📄 **test_api_endpoints.sh** - Проверяет API сервиса
- 📄 **TELEGRAM_SERVICE_GUIDE.md** - Полное руководство (на случай, если что-то пойдёт не так)

---

## 🔍 ЧТО ДЕЛАЕТ КАЖДЫЙ СКРИПТ

### 1. telegram_diagnostic.sh (Диагностика)
Проверяет:
- ✅ Работают ли Docker контейнеры
- ✅ Правильные ли API ключи Telegram
- ✅ Доступен ли веб-интерфейс
- ✅ Есть ли ошибки в логах
- ✅ Работает ли база данных

**Как использовать:**
```bash
bash telegram_diagnostic.sh
```

---

### 2. fix_telegram_service.sh (Исправление)
Автоматически:
- 🛑 Останавливает сервис
- 💾 Создаёт бэкап данных
- ⚙️ Проверяет настройки
- 🔨 Пересобирает контейнер
- ▶️ Запускает сервис заново
- ✅ Проверяет, что всё работает

**Как использовать:**
```bash
bash fix_telegram_service.sh
```

---

### 3. test_telegram_auth.py (Тестирование авторизации)
Интерактивный скрипт для проверки:
- 📨 Отправки SMS кода
- 📲 QR авторизации
- 🔐 Существующих сессий

**Как использовать:**
```bash
# Скопировать на сервер
scp test_telegram_auth.py root@83.222.25.94:/tmp/

# Запустить на сервере
ssh root@83.222.25.94 "docker cp /tmp/test_telegram_auth.py telegram-multi-service:/tmp/ && docker exec -it telegram-multi-service python3 /tmp/test_telegram_auth.py"
```

Следуйте инструкциям на экране!

---

### 4. test_api_endpoints.sh (Тестирование API)
Проверяет все публичные адреса вашего сервиса:
- 🌐 https://telegram.afonin-lisa.ru/
- 🔌 https://tg.api.afonin-lisa.ru/
- 📊 Health checks
- 📝 API документация

**Как использовать:**
```bash
bash test_api_endpoints.sh
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

### Q: Где будут файлы после скачивания?

**Windows:** `C:\Users\ВашеИмя\telegram_service_fix\`

**Linux:** `~/telegram_service_fix/`

---

### Q: Как запустить скрипты из Windows?

У вас есть 3 варианта:

**Вариант 1: Git Bash (Рекомендуется)**
1. Установите Git для Windows: https://git-scm.com/download/win
2. Щёлкните правой кнопкой в папке с файлами
3. Выберите "Git Bash Here"
4. Введите: `bash telegram_diagnostic.sh`

**Вариант 2: WSL (Windows Subsystem for Linux)**
1. Установите WSL: https://docs.microsoft.com/en-us/windows/wsl/install
2. Откройте Ubuntu из меню Пуск
3. Перейдите в папку: `cd /mnt/c/Users/ВашеИмя/telegram_service_fix`
4. Введите: `bash telegram_diagnostic.sh`

**Вариант 3: Запуск напрямую на сервере через SSH**
```cmd
ssh root@83.222.25.94 "bash -s" < telegram_diagnostic.sh
```

---

### Q: Что делать, если не работает bash?

Используйте команду напрямую через SSH:

```cmd
ssh root@83.222.25.94 "bash -s" < telegram_diagnostic.sh
```

Это запустит скрипт на сервере, не требуя bash на вашем компьютере.

---

### Q: Нужно ли мне знать программирование?

**НЕТ!** Просто скопируйте и вставьте команды из этой инструкции.

---

### Q: Что делать, если скрипт просит пароль?

Это пароль от вашего сервера (SSH). Введите тот же пароль, который используете для команды `ssh root@83.222.25.94`.

---

### Q: Можно ли запустить всё через один клик?

Да! Используйте автоматическую установку (ВАРИАНТ 1) - она сделает всё за один раз.

---

## 🆘 ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: "curl не найден"
**Решение для Windows:**
```powershell
# Установите через winget
winget install curl.curl
```

**Решение для Linux:**
```bash
# Ubuntu/Debian
sudo apt-get install curl

# CentOS/RHEL
sudo yum install curl
```

---

### Проблема: "bash не найден" (Windows)
**Решение:**
Установите Git Bash: https://git-scm.com/download/win

Или используйте команду:
```cmd
ssh root@83.222.25.94 "bash -s" < telegram_diagnostic.sh
```

---

### Проблема: "Permission denied"
**Решение для Linux:**
```bash
chmod +x telegram_diagnostic.sh
chmod +x fix_telegram_service.sh
chmod +x test_api_endpoints.sh
```

---

### Проблема: "Connection refused" при SSH
**Решение:**
1. Проверьте, что у вас правильный IP: `83.222.25.94`
2. Проверьте, что порт 22 открыт
3. Попробуйте: `ssh -v root@83.222.25.94` для подробной диагностики

---

## 📞 ЧТО ДАЛЬШЕ?

После запуска диагностики вы получите отчёт. В зависимости от результата:

### ✅ Если всё работает:
Откройте https://telegram.afonin-lisa.ru/ и попробуйте:
1. Зарегистрироваться
2. Создать профиль Telegram
3. Авторизоваться по QR коду

### ⚠️ Если есть проблемы:
Запустите исправление:
```bash
bash fix_telegram_service.sh
```

### ❌ Если ничего не помогает:
1. Откройте файл `TELEGRAM_SERVICE_GUIDE.md`
2. Там есть полное руководство по всем возможным проблемам
3. Или покажите мне вывод диагностики - я помогу!

---

## 💡 СОВЕТ

Сохраните эту инструкцию! Вы можете использовать эти скрипты в любое время для проверки состояния сервиса.

---

## 🎉 ГОТОВО!

Теперь у вас есть всё необходимое для исправления проблем с Telegram сервисом.

**Одна команда решает всё:**

### Windows PowerShell:
```powershell
iwr -useb "https://raw.githubusercontent.com/ankor1403/ankor1403/claude/audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm/install_and_run.bat" -OutFile "$env:TEMP\install_and_run.bat"; & "$env:TEMP\install_and_run.bat"
```

### Linux/Mac Terminal:
```bash
curl -sSL "https://raw.githubusercontent.com/ankor1403/ankor1403/claude/audit-dependencies-mkbp0op9n8ha6qpk-Gt2Dm/install_and_run.sh" | bash
```

**Просто скопируйте, вставьте и нажмите Enter!** 🚀
