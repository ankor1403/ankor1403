# 🤖 ИНСТРУКЦИЯ ПО УСТАНОВКЕ Л.И.С.А. (LISA)

## 📋 ЧТО ТАКОЕ LISA?

**Л.И.С.А.** — это бесплатная self-hosted AI платформа для развертывания собственного ИИ-решения на вашем сервере.

### Включает:
- 🤖 **N8N** — автоматизация с 400+ интеграциями
- 🧠 **Ollama** — локальные AI модели (Llama, Mistral, Gemma)
- 💾 **Supabase** — PostgreSQL база с векторным поиском
- 🔍 **Qdrant** — векторное хранилище для RAG
- 🎤 **Whisper** — распознавание речи
- 🔒 **Caddy** — автоматический HTTPS
- 🎬 **FFmpeg** — обработка аудио/видео

---

## ⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ!

### ❌ КОНФЛИКТ ПОРТОВ!

На вашем сервере **83.222.25.94** уже работают сервисы:
- **wappi-caddy** — занимает порты **80** и **443**
- **telegram-multi-service** — порт **8200**
- **telegram-service** — порт **8100**

**LISA ТОЖЕ ИСПОЛЬЗУЕТ ПОРТЫ 80 и 443!**

### 🔥 ПРОБЛЕМА:
Если установить Lisa на этот сервер, произойдет **конфликт портов** и один из сервисов перестанет работать.

---

## 💡 ВАРИАНТЫ РЕШЕНИЯ

### Вариант 1: ⭐ УСТАНОВКА НА ДРУГОЙ СЕРВЕР (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Нет конфликтов портов
- ✅ Независимая работа сервисов
- ✅ Проще управлять
- ✅ Лучше производительность

**Требования для нового сервера:**
- CPU: 2 ядра (рекомендуется 4+)
- RAM: 8 ГБ (рекомендуется 12 ГБ)
- Диск: 50 ГБ SSD
- ОС: Ubuntu 20.04+ / Debian 11+
- Свободные порты: 22, 80, 443, 8001, 8005, 5432, 11434

**Где арендовать дешевый VPS:**
- Beget (рекомендуется автором Lisa)
- Timeweb
- REG.RU
- Hetzner

---

### Вариант 2: 🛠️ ОСТАНОВИТЬ СУЩЕСТВУЮЩИЙ CADDY

**ВНИМАНИЕ:** Это остановит telegram.afonin-lisa.ru!

```bash
# Остановить Caddy:
cd /opt/wappi
docker-compose down

# После установки Lisa можно настроить один Caddy для всех сервисов
```

---

### Вариант 3: 🔧 ИЗМЕНИТЬ ПОРТЫ LISA

Можно настроить Lisa на использование других портов (сложнее, требует ручной правки конфигов).

---

## 🚀 УСТАНОВКА LISA (ДЛЯ НОВОГО СЕРВЕРА)

### ШАГ 1: Подключитесь к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
```

---

### ШАГ 2: Обновите систему

```bash
apt update && apt upgrade -y
```

---

### ШАГ 3: Установите необходимые пакеты

```bash
apt install -y git curl python3 python3-pip
```

---

### ШАГ 4: Установите Docker

```bash
# Скачать официальный скрипт установки Docker:
curl -fsSL https://get.docker.com -o get-docker.sh

# Установить Docker:
sh get-docker.sh

# Добавить пользователя в группу docker:
usermod -aG docker $USER

# Проверить установку:
docker --version
docker-compose --version
```

---

### ШАГ 5: Перезапустите сессию SSH

Выйдите и зайдите заново, чтобы применились права docker:

```bash
exit
ssh root@ВАШ_IP_АДРЕС
```

---

### ШАГ 6: Скачайте Lisa

```bash
cd ~
git clone https://github.com/shorin-nikita/lisa.git
cd lisa
```

---

### ШАГ 7: Запустите установку

```bash
python3 CTAPT.py
```

---

### ШАГ 8: Ответьте на вопросы скрипта

Скрипт спросит:

#### 1. **Домен для N8N** (автоматизация)
```
Например: n8n.ваш-домен.ru
Или оставьте пустым для доступа по IP:8001
```

#### 2. **Домен для Supabase** (база данных)
```
Например: supabase.ваш-домен.ru
Или оставьте пустым для доступа по IP:8005
```

#### 3. **Домен для Ollama** (AI модели)
```
Например: ollama.ваш-домен.ru
Или оставьте пустым для доступа по IP:11434
```

#### 4. **Email для SSL сертификатов**
```
Если указали домены, введите ваш email для Let's Encrypt
```

#### 5. **Настройки прокси** (опционально)
```
Если нужен прокси-сервер, введите данные
Иначе пропустите
```

---

### ШАГ 9: Дождитесь завершения

Скрипт автоматически:
- ✅ Определит GPU (если есть)
- ✅ Сгенерирует пароли и ключи
- ✅ Настроит firewall
- ✅ Запустит все контейнеры
- ✅ Настроит SSL (если указали домены)

Процесс займет **5-10 минут**.

---

### ШАГ 10: Проверьте установку

```bash
docker ps
```

Вы должны увидеть контейнеры:
- n8n
- supabase-*
- ollama (если настроен)
- postgres
- caddy
- и другие

---

## 🌐 ДОСТУП К LISA

### Если использовали домены:
- **N8N (автоматизация):** https://n8n.ваш-домен.ru
- **Supabase (база данных):** https://supabase.ваш-домен.ru
- **Ollama (AI модели):** https://ollama.ваш-домен.ru

### Если БЕЗ доменов:
- **N8N:** http://ВАШ_IP:8001
- **Supabase:** http://ВАШ_IP:8005
- **Ollama:** http://ВАШ_IP:11434

---

## 🔑 ПОЛУЧЕНИЕ ПАРОЛЕЙ

После установки скрипт выведет учетные данные. Также их можно посмотреть в файле:

```bash
cd ~/lisa
cat .env | grep -E "(PASSWORD|SECRET|JWT)"
```

---

## 🎯 ПЕРВЫЕ ШАГИ ПОСЛЕ УСТАНОВКИ

### 1. Войдите в N8N

Откройте N8N по адресу (домен или IP:8001) и создайте администратора.

### 2. Импортируйте готовые workflows

В Lisa есть 4 готовых шаблона:
- 🎙️ Голосовой ассистент (Whisper + Ollama)
- 📄 RAG с векторной базой
- 🤖 Telegram бот с AI
- 📧 Email автоматизация

**Где найти:**
```bash
cd ~/lisa/n8n/backup/workflows/
ls -la
```

В N8N: Settings → Import from File → выберите .json файл

### 3. Скачайте AI модели в Ollama

```bash
# Зайти в контейнер Ollama:
docker exec -it ollama bash

# Скачать модель (например, Llama 3.1):
ollama pull llama3.1

# Посмотреть доступные модели:
ollama list

# Выйти:
exit
```

**Популярные модели:**
- `llama3.1` — универсальная модель (4.7 ГБ)
- `mistral` — быстрая модель (4.1 ГБ)
- `gemma2` — компактная модель (1.6 ГБ)

### 4. Настройте Supabase

Откройте Supabase по адресу и войдите с паролем из `.env`.

---

## 📊 УПРАВЛЕНИЕ LISA

### Посмотреть статус:
```bash
cd ~/lisa
docker ps
```

### Остановить все:
```bash
cd ~/lisa
docker-compose down
```

### Запустить все:
```bash
cd ~/lisa
docker-compose up -d
```

### Перезапустить все:
```bash
cd ~/lisa
docker-compose restart
```

### Посмотреть логи:
```bash
cd ~/lisa
docker-compose logs -f
```

### Посмотреть логи конкретного сервиса:
```bash
docker logs -f n8n
docker logs -f ollama
docker logs -f supabase-kong
```

---

## 🔄 ОБНОВЛЕНИЕ LISA

```bash
cd ~/lisa
python3 O6HOBA.py
```

---

## 🆘 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема 1: "docker: permission denied"

```bash
# Добавить пользователя в группу docker:
sudo usermod -aG docker $USER

# Выйти и зайти заново:
exit
ssh root@ВАШ_IP
```

### Проблема 2: "порт уже занят"

```bash
# Посмотреть что использует порт:
netstat -tlnp | grep :80
# Или:
ss -tlnp | grep :80

# Остановить конфликтующий сервис:
docker stop ИМЯ_КОНТЕЙНЕРА
```

### Проблема 3: Контейнеры не запускаются

```bash
# Посмотреть логи:
cd ~/lisa
docker-compose logs

# Пересоздать контейнеры:
docker-compose down
docker-compose up -d
```

### Проблема 4: Нет доступа к веб-интерфейсам

```bash
# Проверить открытые порты:
netstat -tlnp | grep -E ":(80|443|8001|8005)"

# Проверить firewall:
ufw status

# Открыть порты:
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 8001/tcp
ufw allow 8005/tcp
```

### Проблема 5: SSL не работает

```bash
# Проверить логи Caddy:
docker logs caddy

# Убедитесь что:
# 1. Домены правильно прописаны в DNS (A-запись на IP сервера)
# 2. Порты 80 и 443 открыты
# 3. Email указан корректно
```

---

## 📁 СТРУКТУРА LISA

```
~/lisa/
├── CTAPT.py               ← Скрипт установки
├── O6HOBA.py              ← Скрипт обновления
├── docker-compose.yml     ← Главная конфигурация
├── .env                   ← Переменные окружения (ПАРОЛИ!)
├── n8n/
│   └── backup/
│       └── workflows/     ← Готовые шаблоны автоматизации
├── supabase/              ← Конфиги базы данных
├── ollama/                ← AI модели
└── caddy/                 ← SSL сертификаты
```

---

## 🔐 БЕЗОПАСНОСТЬ

### ⚠️ ВАЖНО:

1. **Защитите файл .env:**
```bash
chmod 600 ~/lisa/.env
```

2. **Смените пароли по умолчанию:**
Отредактируйте `.env` и перезапустите:
```bash
cd ~/lisa
nano .env
docker-compose restart
```

3. **Настройте firewall:**
```bash
# Разрешить только необходимые порты:
ufw allow 22/tcp   # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp  # HTTPS
ufw enable
```

4. **Регулярно обновляйте:**
```bash
cd ~/lisa
python3 O6HOBA.py
```

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

- 📺 **Видео-инструкция:** https://youtu.be/vEwYIoZAFOY
- 📖 **Блог автора:** https://aibot.direct/blog/lisa-install
- 💻 **GitHub репозиторий:** https://github.com/shorin-nikita/lisa
- 🤝 **Сообщество:** (ссылки в README репозитория)

---

## 💡 СОВЕТЫ

### Для лучшей производительности:

1. **Используйте SSD диск** (не HDD)
2. **Минимум 12 ГБ RAM** для комфортной работы с AI моделями
3. **GPU значительно ускорит** работу Ollama (но не обязателен)
4. **Используйте домены** для удобного доступа и автоматического SSL

### Экономия ресурсов:

1. **Скачивайте только нужные модели** в Ollama (они большие!)
2. **Используйте компактные модели** (gemma2, mistral) вместо больших (llama3.1:70b)
3. **Останавливайте неиспользуемые контейнеры:**
```bash
docker stop ollama  # Если не используете AI модели
```

---

## 🎉 ГОТОВО!

После установки у вас будет:
- ✅ Своя система автоматизации (N8N)
- ✅ Локальные AI модели (Ollama)
- ✅ Векторная база для RAG (Supabase + Qdrant)
- ✅ Распознавание речи (Whisper)
- ✅ Автоматический HTTPS (Caddy)

Всё это **БЕСПЛАТНО** и **БЕЗ ПОДПИСОК**!

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

**Q: Сколько стоит Lisa?**
A: Lisa полностью бесплатна. Платить нужно только за сервер.

**Q: Можно ли установить Lisa на домашний компьютер?**
A: Да, но нужен статический IP или VPN для доступа извне.

**Q: Работает ли Lisa без GPU?**
A: Да, но AI модели будут работать медленнее (на CPU).

**Q: Можно ли использовать Lisa коммерчески?**
A: Да, лицензия Apache 2.0 позволяет.

**Q: Как добавить свои workflows?**
A: Создайте их в N8N и экспортируйте в JSON.

---

## 📧 НУЖНА ПОМОЩЬ?

Если что-то не получается:
1. Посмотрите логи: `docker-compose logs`
2. Проверьте README: https://github.com/shorin-nikita/lisa
3. Посмотрите видео: https://youtu.be/vEwYIoZAFOY
4. Напишите в issues на GitHub

---

**Удачи с Lisa! 🤖🚀**
